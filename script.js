"use strict";

// Functions: Write functions to perform basic tasks such as calculating the area of a rectangle, converting temperatures, or reversing a string.

function getRectArea(a, b, c) {
  return `rectangle area is : ${a + b + c}`;
}
